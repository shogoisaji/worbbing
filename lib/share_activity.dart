import 'package:flutter/material.dart';

class ShareActivity extends StatelessWidget {
  const ShareActivity({super.key});

  @override
  Widget build(BuildContext context) {
    // final sharedText = ModalRoute.of(context)?.settings.arguments as String;

    // Navigator.of(context).pushNamed('/registration', arguments: sharedText);
    print('share activity');
    return Container();
  }
}
